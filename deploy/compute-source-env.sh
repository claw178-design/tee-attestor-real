#!/usr/bin/env sh
# Placeholder — EigenCompute injects env vars at runtime.
# This script sources any KMS-provided secrets into the environment.
echo "[compute-source-env] EigenCompute environment loaded"
